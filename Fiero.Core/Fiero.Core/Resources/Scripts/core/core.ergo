:- module(core, []).


io:portray('\n') :- write_raw('\n'), !.
%: Overrides stdout writing to automagically include the name of the calling script.
io:portray(X) :-
    current_module(M),
    write_raw(M, ': ', X).