:- module(map_test, []).
:- use_module(fiero).
:- use_module(random).
:- use_module(map).
:- use_module(prefabs_test).
:- map(25, 25).

step{ size: p(W, H) } #-> [
	fill_rect(room, p(W, H), p(0, 0)),
	draw_rect(wall, p(W, H), p(0, 0))
].

step{ size: p(W, H) } #-> [
	place_prefab(spawn, _{center_y: true, center_x: true}, p(:=(W/2), :=(H/2)))
].

step{ size: p(W, H) } #-> [
	place_prefab(spawn, _{}, p(I, 0)),
	place_prefab(spawn, _{}, p(I, :=(H-4)))
] :- 
	range(0 <= I < W, 5).