:- module(map_test, []).
:- use_module(fiero).
:- use_module(random).
:- use_module(map).
:- use_module(prefabs_test).
:- map(_{
	size: p(49, 49),
	pools: _{
		monster: [
			npc_rat, 
			npc_rat_knight
		],
		item: [
			potion_of_healing, 
			potion_of_confusion, 
			weapon_sword, 
			weapon_spear, 
			weapon_hammer
		]
	}
}).

step{ size: p(W, H) } #-> [
	fill_rect(room, p(W, H), p(0, 0)),
	draw_rect(wall, p(W, H), p(0, 0))
].

step{ size: p(W, H) } #-> [
	place_prefab(_, _, _{ tags: {spawn}, center_y: true, center_x: true}, p(:=(W/2), :=(H/2)))
].

step{ } #-> [
	place_prefab('2x2', _, _{ randomize: true }, p(5, 5)),
	place_prefab('2x2', _, _{ randomize: true }, p(25, 5)),
	place_prefab('2x2', _, _{ randomize: true }, p(5, 35)),
	place_prefab('2x2', _, _{ randomize: true }, p(35, 35))
].
