:- module(test, []).
:- use_module(input).
:- observe([view_window_size]).

input:key_route(j, pressed, [_] >> j_pressed).

data:view_window_size_changed(OldValue, NewValue) :-
	write(OldValue), nl,
	write(NewValue), nl, nl.

j_pressed :-
	set(view_window_size, _, p(1200, 1200)).