:- module(map_test, []).
:- use_module(fiero).
:- use_module(random).
:- use_module(map).
:- use_module(prefabs_test).
:- map(25, 25).

step{ size: p(W, H) } #-> [
	fill_rect(room, p(W, H), p(0, 0)),
	draw_rect(wall, p(W, H), p(0, 0))
].

step{ } #-> [
	place_prefab(spawn, _{}, p(3, 3))
].