:- module(test, []).
:- use_module(input).
:- subscribe(action, [actor_moved]).

test:began :- 
	bind(k, pressed, [E]>>spawn(npc_rat)).
test:ended :- unbind(_, _, _).

action:actor_moved(_ { actor: @owner }) :- 
	position(@owner, P),
	animate(false, P, [buff{tint: light_green}]).
